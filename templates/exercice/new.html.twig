<!DOCTYPE html>

<title>New Exercice</title>

{% block body %}
    <h1>Create new Exercice</h1>

    {{ include('exercice/_form.html.twig') }}

    <a href="{{ path('app_exercice_index') }}">back to list</a>
{% endblock %}
